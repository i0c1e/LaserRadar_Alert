create table model_args(
    id INTEGER PRIMARY KEY autoincrement,
    kernel_size INTEGER NOT NULL,
    create_time INTEGER NOT NULL
);
insert into model_args(
    kernel_size,create_time
    )values(0,)